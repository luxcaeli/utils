## minMaxBetween
Check whether value is classified as a function object.
```typescript
if(isFunction(onClick)){
    onClick(event);
}
```

## Usage
 ```typescript
 if(isFunction(onClick)){
  onClick(event);
 }
 ```
